import styled from 'styled-components';

export const Div = styled.div`
  div .form {
    max-width: 900px;
  }

  button.addPessoa {
    float: right;
  }

  button.cancelar {
    margin-left: 5px;
  }

  p.form-err {
    color: #ff0000;
    margin-top: 40px;
    font-size: 25px;
    text-align: center;
  }
`;
